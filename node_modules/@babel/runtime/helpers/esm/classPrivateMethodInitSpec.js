import "./checkPrivateRedeclaration.js"
import checkPrivateRedeclaration

export default function _classPrivateMethodInitSpec(obj, privateSet) {
  checkPrivateRedeclaration(obj, privateSet);
  privateSet.add(obj);
}