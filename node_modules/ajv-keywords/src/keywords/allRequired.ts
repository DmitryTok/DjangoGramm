import "../definitions/allRequired"
import "ajv"
import getDef
import type
import {Plugin}

const allRequired: Plugin<undefined> = (ajv) => ajv.addKeyword(getDef())

export default allRequired
module.exports = allRequired
